if (project.ext.has("fabricLoaded")) {
    project.ext.effectiveJar = tasks.remapJar
} else {
    project.ext.effectiveJar = tasks.jar
    project.dependencies.ext.modImplementation = { x -> project.dependencies.implementation(x) }
}
